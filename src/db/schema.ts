// Mock Schema - 简化版本，用于避免构建错误
// 实际的数据库 schema 已被删除，这里只是为了满足类型检查

export const gempix2Tasks = null as any;
export const sora2Tasks = null as any;
export const veo3Tasks = null as any;
export const upscalerTasks = null as any;
export const wan25Tasks = null as any;
